# Sonatype Demo App - Hugging Face Transformers
# Intentionally vulnerable versions for demo purposes
# Note: torch removed due to size (2GB+) causing ngrok timeouts in CI

transformers==4.30.0
tokenizers==0.23.1
huggingface-hub==0.14.1
numpy==1.24.0
pillow==9.3.0
requests==2.26.0
